const STATUS = require('./status');
const HTTP_STATUS = require('./http-status');
const TYPE = require('./type');
const ADDRESS_TYPE = require('./address-type');
const USER_TYPE = require('./user-type');
const OTP_STATUS = require('./otp-status');
const PAYMENT_STATUS = require('./payment-status');
const ORDER_STATUS = require('./order-status');
const DELIVERY_TYPE = require('./delivery-type');

module.exports = {
  TYPE,
  STATUS,
  USER_TYPE,
  OTP_STATUS,
  HTTP_STATUS,
  ADDRESS_TYPE,
  PAYMENT_STATUS,
  ORDER_STATUS,
  DELIVERY_TYPE,
};
