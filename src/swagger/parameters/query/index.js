const ids = require('./ids');
const pageNo = require('./page-no');
const pageSize = require('./page-size');
const search = require('./search');
const sortBy = require('./sort-by');
const sortOrder = require('./sort-order');
const status = require('./status');

module.exports = {
  ids,
  pageNo,
  pageSize,
  search,
  sortBy,
  sortOrder,
  status,
};
